# Release status

Current status: **Candidate** — tutorial source complete, awaiting clean-runtime execution. The pipeline package, offline unit tests (including the role-helper and notebook-parity suites), one executed CPU smoke run from the pinned snapshot, `MODEL_CARD.md`, and the standalone `TASK-INFERENCE` tutorial notebook `tutorials/smolvlm_vision_language_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 1.1 §3.6) with its static validator and CI workflow exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR3). Unit tests and static conformance checks are expected to run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
