# Release status

Current status: **Candidate — tutorial source complete, model-backed pre-flight executed, awaiting supported-runtime execution**. The pipeline package (image + text chat generation plus the adaptation contract for one instruction: the Belfort line sample, corpus CER/WER, bounded fine-tuning of the last four decoder layers, the verified adapter), offline unit tests (including the adaptation, role-helper, import-boundary and notebook-parity suites), the model-backed suite, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/smolvlm_vision_language_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR4). The earlier `TASK-INFERENCE` notebook's Colab T4 run (2026-09-13) is retained as history and is not evidence for the `E2E` blob; the repository is not release-grade until a Colab/Kaggle execution of the exact candidate notebook blob is recorded in `docs/release-verification.md`.
