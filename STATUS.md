# Release status

Current status: **Candidate** — tutorial source complete, awaiting clean-runtime execution. The pipeline package, offline unit tests, one executed CPU smoke run from the pinned snapshot, `MODEL_CARD.md`, and the `TASK-INFERENCE` tutorial notebook `tutorials/smolvlm_vision_language_colab.ipynb` with its static validator and CI workflow exist. Unit tests and static conformance checks are expected to run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
