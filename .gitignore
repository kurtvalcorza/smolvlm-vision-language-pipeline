.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
outputs/
.DS_Store

# Model weights are pulled locally; only small config/tokenizer files are committable
weights/**/*.safetensors
weights/**/*.ckpt
weights/**/*.bin
weights/**/*.pt
weights/**/*.pth
weights/**/*.onnx
weights/**/.cache/

# Retained verification exports are evidence, not local runtime output
!docs/verification/2026-09-13/outputs/
!docs/verification/2026-09-13/outputs/**

# Belfort-line row-group cache (fetched at run time, digest-pinned)
weights/belfort/
